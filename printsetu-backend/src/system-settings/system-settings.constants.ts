/**
 * Well-known system_settings keys (SRS §6 "System settings"). Global,
 * platform-wide knobs — distinct from print_settings, which is per-shop
 * (see shops/dto/shop.dto.ts UpdatePrintSettingsDto for the per-shop
 * retention/upload-size equivalents).
 */
export const DEFAULT_RETENTION_MINUTES_KEY = 'DEFAULT_RETENTION_MINUTES';
